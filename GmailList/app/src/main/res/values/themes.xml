<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light theme -->
    <style name="Theme.GmailList" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Tùy biến thêm nếu thích -->
        <item name="colorPrimary">@android:color/holo_red_dark</item>
        <item name="colorPrimaryVariant">@android:color/holo_red_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- BẮT BUỘC để AppCompat không đòi ActionBar -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

</resources>